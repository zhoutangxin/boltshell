# 终端删除键的跨平台兼容性说明
## 1. 问题背景
在 SSH 终端场景中，经常会遇到这样的问题：

- 在本机上按下 Backspace / Delete 后，本地界面有反应，但远端 Linux 终端并没有正确执行“删除”操作。
- 同一个程序在不同操作系统（Windows / macOS / Linux）上运行时，删除键行为不一致。
  本质上，这是一个 “输入端”和“接收端”对删除键编码理解不一致 的兼容性问题。

## 2. 输入链路概览
从你按下键盘上的一个键，到远端 Linux 收到信号，大致会经历这样一条链路：

1. 键盘硬件
2. 操作系统 （Windows / macOS / Linux）
3. 图形/输入库 （这里是 Gio）
4. 应用程序 （ssh-go GUI）
5. SSH 协议 / 客户端
6. 远端 Linux Shell （bash / zsh 等）
   任何一环对“删除键”的编码/解释不一致，都会导致最终行为不一致。

## 3. 差异来源
### 3.1 输入端差异（本机）
不同操作系统和终端习惯，对“删除键”使用的编码不同，常见有两种：

- 0x08 ： ^H ，历史上很多终端把它当作 Backspace。
- 0x7f ： ^? （DEL），很多现代系统默认用它作为删除键。
  再加上图形库（比如 Gio）有自己的抽象：

- 可能把 Backspace 表示为某个 特殊的 key.Name ；
- 也可能直接给出一个 Unicode 控制字符 ；
- 某些环境下，还可能出现你看到的那种“长度为 3 的奇怪字符”（比如某个特殊符号或组合编码）。
  所以，从程序的角度看： “删除键”不一定稳定表现为同一个字符/名字 。

### 3.2 接收端差异（远端 Linux）
服务器端的终端配置（ stty ）决定了它把什么当作“删除”：

- 很多系统默认认为 ^? (0x7f) 是删除；
- 一些老环境或特殊配置会认为 ^H (0x08) 才是删除；
- 这可以通过 stty -a 查看当前配置。
  如果本地发的是 0x08 ，而远端只认 0x7f ，就会出现“按了删除键但远端不删”的现象；反之亦然。

## 4. 标准化映射方案
解决的核心思路是：在应用内部做一层 “标准化映射” ，把各种输入端差异统一成远端能正确识别的编码。

### 4.1 捕获
在 GUI 应用中，先尽可能“宽松地”捕获所有可能的删除键表现形式：

- 常规的 key.NameDeleteBackward 、 key.NameDeleteForward ；
- 控制字符： \b (0x08)、 0x7f ；
- 某些平台下的特殊 Unicode 字符（例如你看到的“长度为 3 的字符”，通过日志里的 hex 可以定位）。
  只要能确认“这是用户想要的删除动作”，就算它的编码看起来很奇怪，也要记下来。

### 4.2 归一化
无论捕获到的是哪种形式，在发送给远端前，统一做转换：

- 统一发送为 \x7f （DEL） ：
    - 大部分现代 Linux 默认把 ^? 当作删除；
    - 如果远端 stty 设置正确，基本都能正常工作。
- 必要时根据配置切换：
    - 提供一个简单选项，让用户选择发送 ^H ( 0x08 ) 还是 ^? ( 0x7f )；
    - 这类似很多终端工具（Putty / Xshell）里那种“Backspace/Del 键行为”的设置。
      通过这一步，把“输入端五花八门的编码”压缩成“远端单一、稳定的编码”。

### 4.3 兜底配置
为极少数特殊环境预留一个兜底策略：

- 如果发现某些服务器/终端仍然不能正常删除：
    - 可以让用户在应用里切换“删除键模式”：
        - 模式 A：发送 0x7f （适配大部分 Linux 默认）
        - 模式 B：发送 0x08 （适配某些老系统或特殊配置）
- 这样就不用每次改代码，只要调配置即可。
## 5. 调试与日志建议
为了定位具体的兼容性问题，建议在本地加上 详细日志 ：

- 记录键盘事件的：
    - 名称： key.Name
    - 长度： len(...)
    - 十六进制编码： %x 打印底层字节
- 根据日志里的 hex=... 数值，可以准确知道某个“神秘字符”的真实编码，然后再在代码里针对性映射到 \x7f 。
  这样就可以做到：

1. 在你自己的机器上，用日志确认“删除键”到底是什么编码；
2. 在其他人机器遇到问题时，让他们发一段日志，你根据 hex 值扩展映射逻辑；
3. 逐步形成一个“跨平台删除键映射表”，代码越来越健壮。
## 6. 总结
- 差异原因 ：不同操作系统 / 终端 / 配置，对删除键使用不同编码 ( 0x08 vs 0x7f )，再加上图形库抽象，导致输入端不一致；远端 Linux 的 stty 又决定了它认哪一种为删除。
- 核心方案 ：在应用层做一层“标准化映射”：
    - 尽可能广泛地捕获各种“删除键”表现；
    - 在发送给远端前统一转换为 \x7f （或通过配置选择 0x08 ）；
    - 必要时提供模式开关，解决少数特殊环境。
- 好处 ：一旦这层映射写好，程序在 Windows / macOS / Linux 上行为就会稳定下来，不需要用户手动去折腾 stty 或更换终端工具。