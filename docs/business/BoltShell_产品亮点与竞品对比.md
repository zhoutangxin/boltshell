# BoltShell 产品亮点、竞品对比、待做清单、宣传与 Pro

> 更新：2026-08-21  
> 前提：**仅一人维护**；先打磨可宣传的差异，再补信任与迁移，最后才堆 Pro 功能。  
> 关联：[个人开发者商业化 FAQ](BoltShell_个人开发者商业化FAQ.md) · [商业化与开源策略](BoltShell_商业化与开源策略.md) · [品牌与产品规划](../product/BoltShell_品牌与产品规划文档.md) · [TODO](../TODO.md)

---

## 一、结论先看

| 问题 | 答案 |
|------|------|
| 现在就能打的亮点？ | **① 跨服务器直传文件** ② **终端 + SFTP + 监控同屏** ③ 体验细节（如密码可见切换）④ 克制赞助（非操作区） |
| Pro 还去不去赞助？ | **不去**。Pro 卖真功能；赞助全员可见 |
| 一人维护先做什么？ | **密钥登录 → 密码 SQLite 加密 → HostKey → FinalShell 导入 → 隐私/抓包页** |
| 怎么宣传？ | 只打跨机传 + 一体化；学安利文结构，**能力表诚实**；不抄「Rust 高性能 / FinalShell 有广告」 |
| Pro 额外功能？ | Snippets、会话录制、分屏、端口转发、CLI 增强等——**不是**去赞助、不是限连接解锁 |
| 别宣传成已有的？ | FinalShell/Xshell 一键导入、密钥登录、网络实时图、批量多机、产品级 `bolt` CLI |

---

## 二、已定商业规则：Pro 不去赞助

| 档位 | 赞助位 | 卖什么 |
|------|--------|--------|
| Free | 可见 | 完整核心运维（含跨机传、监控） |
| 支持者 ¥9.9 | 可见 + 徽章/致谢 | 支持开发（可不解锁功能） |
| **Pro ¥88 买断** | **仍可见**（可关单条，「关闭推荐」） | §八 真功能清单 |

代码若仍 `IsPro → 隐藏全部赞助`，属旧逻辑，应改掉。

**徽章：** 非实体——关于页「支持者」、侧栏极小标、官网 `/thanks` 昵称（可匿名）。

---

## 三、产品亮点（按「能不能当标题」排序）

### 3.1 主亮点（已具备，宣传主线只打这两条）

| # | 亮点 | 用户一句话 | 为何多数竞品弱 |
|---|------|------------|----------------|
| 1 | **跨服务器直传文件** | 不用先下到电脑，A 机文件直接到 B 机 | 见 §3.4 |
| 2 | **终端 + SFTP + 监控一体** | 一个窗口连上就能敲命令、传文件、看 CPU/内存 | Tabby/WindTerm 偏终端；纯 SFTP 无监控同屏 |

### 3.2 辅助亮点（有，放「体验」里，不当唯一标题）

| 亮点 | 说明 |
|------|------|
| 多 Tab / 分组管理 | 标配 |
| 远端文件在线编辑 | 加分 |
| 终端 GB18030 兜底 | 国内加分 |
| **编辑连接时密码可见切换**（🙈） | 体验加分，少输错；**不是安全卖点**；与「落盘加密」是两回事 |
| 赞助位克制 | 不进终端/文件区/弹窗 |

### 3.3 勿当亮点宣传（尚未达标）

| 文档/文案曾写 | 实际 |
|---------------|------|
| 网络实时图表 | 仅有 CPU/内存/Swap/磁盘/Top 进程 |
| FinalShell / Xshell 一键导入 | 仅有**自有 JSON** 导入导出 |
| 密钥登录 / ssh-agent | 以密码登录为主，**必须补（P0）** |
| 批量多机命令 | 「命令」Tab 多为壳 |
| 密码本地加密存储 | 仍偏明文 SQLite，**必须补（P0）** |

### 3.4 为什么跨机传送「好做」市面却少做？

不是做不到，是**产品边界与成本**：

| 原因 | 说明 |
|------|------|
| 历史模型 | 传统是「本机 ↔ 一台远端」（WinSCP）；A→B 要同时管两台 |
| 定位不同 | Termius 重心多端同步；PuTTY 极简终端；Tabby 偏现代终端 |
| 工程不止「再开 SFTP」 | 目标机未开 Tab、浏览、进度/失败、大文件、双端认证；你们已有连接池 |
| 需求被低估 | 很多人习惯 scp 中转或先下再传 |
| 支持成本 | 「传丢了」比本机上传更难缠，小团队常故意不做 |

→ **你做成了，就反复当主卖点**；不是因为别人做不到。

### 3.5 「不补加密/密钥，读者会觉得又一个 SSH+SFTP」是什么意思？

不是说你「只有 SSH+SFTP 就没价值」。  
是指读者扫功能清单时的**默认归类**：能连 + 能传文件的工具一筐。若看不出「凭据怎么存、能不能密钥登、跨机传/监控」等差异，会懒得细看。  
补密钥/加密 = 跨过「敢不敢当主力」；亮点仍靠跨机传等来讲。

### 3.6 密码存 SQLite 再加密——用户接受吗？

**接受，且常见。** 用户不在意是不是 SQLite，在意：

> 别人拷走数据库文件，能不能直接读出密码？

| 方案 | 接受度 |
|------|--------|
| SQLite 字段 AES-GCM 密文 + 主密钥在 OS Keychain/DPAPI | ✅ 推荐 |
| SQLCipher 整库加密 | ✅ |
| 明文 SQLite | ❌ |

继续用 SQLite 完全没问题。

---

## 四、与市面主流对比

图例：✅ 有 / ⚠️ 弱或部分 / ❌ 无或很弱 / — 非定位

| 能力 | BoltShell | FinalShell | Xshell+Xftp | MobaXterm | Termius | WindTerm | Tabby | electerm | PuTTY | WinSCP |
|------|-----------|------------|-------------|-----------|---------|----------|-------|----------|-------|--------|
| SSH 终端 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | — |
| 图形 SFTP | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ⚠️ | ✅ | ❌ | ✅ |
| 侧栏监控 | ✅ | ✅ | ⚠️ | ⚠️ | ⚠️ | ⚠️ | ❌ | ⚠️ | ❌ | ❌ |
| **跨机直传 A→B** | ✅ | ⚠️ | ⚠️ | ⚠️ | ⚠️ | ❌/弱 | ❌ | ⚠️ | ❌ | ❌ |
| 密钥/agent | ⚠️ 待补 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 竞品导入 | ❌ 待做 | — | 自有 | ⚠️ | ⚠️ | ⚠️ | ⚠️ | ⚠️ | — | — |
| 云同步 | ❌ 暂不做 | 付费 | — | — | ✅ | ❌ | ⚠️ | ⚠️ | ❌ | ❌ |
| 技术栈印象 | **Go + Wails** | Java | 闭源 | 闭源 | **Electron+TS** | C++/部分开源 | Electron/MIT | Electron/MIT | C | C++ |

### 4.1 Termius 技术栈（性能对比用）

| 项 | Termius Desktop |
|----|-----------------|
| 壳 | **Electron** |
| UI | **React + TypeScript** |
| 其它 | Node.js、xterm.js、原生 **C++** 模块（SSH/PTY 等） |

→ 与 Termius 比内存 = **同类 GUI 对比**，比和系统 `ssh` 比更有说服力。  
你测到同量级 → Go+Wails **不虚**；不必追「Rust 高性能」话术。

### 4.2 Go vs「Rust 高性能」营销

- SSH 客户端瓶颈多在网络、终端渲染、会话数，很少是 Go vs Rust 几个百分点。  
- 「Rust 高性能」多为相对 Java/Electron 臃肿的营销句。  
- 对外建议写：**后端 Go；Mac 上与 Termius 等同档 GUI 同场景内存同量级**——用实测，不跟风语言品牌。

### 4.3 FinalShell「有没有广告」——勿乱写

| 可能有的 | 说明 |
|----------|------|
| 升级高级版、云同步、加速等**自家提示** | 不等于穿山甲式第三方广告 |
| 你本机若没看见弹窗硬广 | **对比文不要写「FinalShell 广告多」** |

宣传只说自己：**无弹窗、无安装捆绑、赞助克制可关**——少贬竞品未核实的点。

### 4.4 XPipe / sshxterm（掘金常见安利）

| | [XPipe](https://github.com/xpipe-io/xpipe) | [sshxterm](https://www.sshxterm.com/) | BoltShell |
|--|------|----------|-----------|
| 开源？ | **是，Open Core**（Apache 2.0 核心 + 专业组件闭源） | 公开资料偏**免费闭源/官网安装包**；勿与 GitHub `sshx`（另一项目）混淆 | 待定 Open Core |
| 主战场 | Docker/K8s/多环境连接中心 | 加密 + 免费无广告叙事 | **跨机传 + 监控同屏** |
| SFTP | 有 | 有（文案含 SFTP + lrzsz） | 同屏图形面板 |

「核心永久免费、企业高阶收费」= 个人免费用满 + B 端另收——**模式可参考**；勿照抄「无广告、无阉割」满话（你有赞助鸣谢；以后若收 Pro 功能会被抠字眼）。

适合你的改写：

> 个人日常：核心能力免费（终端/SFTP/监控/跨机传），不限会话数；无弹窗、无捆绑；侧栏赞助可关。  
> 支持者/Pro：致谢或真功能；企业面议。

---

## 五、产品还要支持哪些（一人维护优先级）

原则：**先信任与迁移（P0），再 Pro 真功能（P1），云/企业后置（P2）。**  
灯塔功能（跨机传、监控）**留在 Free，不阉割卖钱。**

### P0 — 下一阶段只做这些

| 功能 | 为什么 | 归属 |
|------|--------|------|
| **SSH 密钥登录** + ssh-agent | 严肃运维标配；**要做** | Free |
| **SQLite 密码加密** + OS Keychain 主密钥 | 拷库读不出明文 | Free |
| **HostKey / known_hosts** | 勿长期 Ignore | Free |
| **FinalShell 一键导入** | 迁移文章前提 | Free（获客） |
| 赞助：Pro 仍可见；可 dismiss | 商业决策一致 | — |
| 官网抓包示例 + 隐私 | 埋点信任 | — |

### P1 — 有余力 / 可作 Pro

| 功能 | 建议归属 |
|------|----------|
| Xshell 导入 | Free 或迁移工具 |
| **Snippets / 命令片段** | **Pro** |
| **会话日志录制** | **Pro** |
| 分屏 | **Pro**（多 Tab 已在 Free） |
| 端口转发 / 隧道 | **Pro** 或后期 |
| 「查看我上报的数据」 | Free |
| 批量多机命令 | 可慢；做成后可 Pro |
| 监控补网络图 | 可选，对齐旧文案 |
| 密码管理增强（生成/保险库） | 后期；与「密码可见」无关 |

### P2 — 一人暂缓

云 Sync、Team 审计/SSO、完整 `bolt` CLI 生态、主题市场、插件系统。

### 明确不做

- 限 5 连接 / 去赞助当 Pro 主卖点  
- 安装捆绑、弹窗、终端区广告  
- 阉割跨机传/监控逼付费  

---

## 六、产品如何宣传

### 6.1 一句话 / 30 秒

> BoltShell：一体化 SSH 运维客户端——终端、SFTP、监控同屏。  
> **和其他工具最大的不同：服务器之间可以直接传文件，不用先下载到自己电脑。**  
> Go + Wails；Mac 上与 Termius 等同档客户端内存同量级。

### 6.2 渠道与顺序（零预算）

| 优先级 | 动作 |
|--------|------|
| 1 | 知乎/掘金/V2EX/博客园：**跨机传文件** + FinalShell 替代（导入做完再写迁移文） |
| 2 | README / 官网：GIF（跨机传、监控同屏）；勿超前承诺 |
| 3 | 小众软件、异次元、蓝点、少数派投稿 |
| 4 | 持续发版日志 |
| 5 | Show HN / 英文材料（有余力） |

学安利文结构：**痛点 → 亮点 → 对比表 → 下载**；表里只写已实现能力。

### 6.3 禁止宣传口径

| ❌ 不要写 | 原因 |
|----------|------|
| Rust 高性能碾压 | 你是 Go；用实测说话 |
| FinalShell 广告多 | 未核实；你可能根本没看见 |
| 已支持密钥/竞品导入/已加密 | 未做完前禁止 |
| 无广告（若有赞助位） | 改写「无弹窗、赞助可关」 |
| 无阉割（若以后有 Pro 功能） | 改写「核心运维免费」 |

### 6.4 对比话术（诚实）

**vs FinalShell：** 一体化接近；我们强调跨机直传；密钥/导入还在补。  
**vs Termius：** 他强在多端云同步（Electron）；我们强在同屏监控+跨机传，无强制云账号。  
**vs Tabby/WindTerm：** 他们偏终端；我们偏运维工作台。  
**vs sshxterm：** 都有 SSH+SFTP；我们主打跨机直传+监控同屏；密钥/加密要对齐后再硬刚「安全」叙事。

---

## 七、Free / 支持者 / Pro 功能怎么切

### 7.1 Free（拉用户，必须够用）

- SSH（密码；**补完后含密钥**）、多 Tab  
- SFTP、远端编辑、本机↔远端传输  
- **跨服务器直传**、侧栏监控  
- 分组、自有 JSON 导入导出、**FinalShell 导入（做完后）**  
- 密码可见切换、应用内更新  
- 赞助位可见（可关）  
- **不限会话数**（不推荐用连接数卡人）

### 7.2 支持者 ¥9.9

- 不解锁核心功能也可  
- 徽章 + 官网致谢  

### 7.3 Pro 额外功能（买断，卖真代码）

| Pro 功能 | 状态建议 | 付费理由 |
|----------|----------|----------|
| Snippets / 命令片段 | 待做 → Pro | 每天省时间 |
| 会话日志录制 | 待做 → Pro | 排错留痕 |
| 分屏（多会话同视） | 待做 → Pro | 多 Tab 已 Free |
| 端口转发 / 隧道 | 待做 → Pro | 进阶组网 |
| `bolt` CLI 增强 | 待做 → 可 Pro | CLI 用户愿付 |
| 批量多机命令 | 待做 → 可 Pro | 集群效率 |
| ~~去赞助~~ | **否决** | — |
| ~~限连接解锁~~ | **不推荐** | 卖解锁 |

**上架门槛：** 至少 2～3 个上表真功能落地再卖 Pro；空壳 Pro 不要发。

**定价形态（已定倾向）：一次性买断**（如支持者 ¥9.9 + Pro ¥88），**不做**「1 年 ¥20」当主方案。有云 Sync 时再单独卖订阅。详见 §7.6。

### 7.3.1 Pro 功能详解（给自己写 PRD / 官网用）

#### Snippets（命令片段）

把常用命令存成可搜索、可分组的片段库，一键插入或执行到当前终端。

| 对比 | 说明 |
|------|------|
| vs 终端历史 | 历史是随便敲过的；Snippets 是整理过的「收藏夹」 |
| 例子 | `journalctl -u nginx -f`、带变量的检查脚本 |
| 以后可扩展 | 多会话同时执行同一片段（接近批量命令） |

#### 会话录制

记录某次 SSH 终端上的输入/输出，便于事后回看。

| 形态 | 说明 |
|------|------|
| 文本日志 | 实现简单，可检索 |
| 类 asciinema 回放 | 带时间轴「像录像」 |
| 注意 | 可能含敏感输出；本地存、默认可关、使用前提示 |

用途：排错留证、写文档、给同事看操作过程。

#### 分屏

同一窗口内同时显示多个终端区域（左右/上下），而不只靠 Tab 来回切。

```
┌──────────┬──────────┐
│ 机 A 日志 │ 机 B 改配置 │
└──────────┴──────────┘
```

多 Tab = 一次盯一个；分屏 = 对照着看。你们 Free 已有多 Tab → 分屏适合进 Pro。

#### 端口转发 / 隧道

经 SSH 把远端端口映到本机（或反向 / SOCKS）。

| 类型 | 白话 |
|------|------|
| 本地转发 | 浏览器开 `localhost:3306` → 实际是服务器内网 MySQL |
| 远程转发 | 让远端访问你本机某服务 |
| 动态 SOCKS | 浏览器走 SSH 当代理访问内网 |

用途：数据库/面板不对公网开放时的安全访问。进阶运维刚需。

#### CLI 增强（`bolt`）

系统终端里复用 GUI 同一套连接库，例如 `bolt list` / `bolt connect prod-api`。  
与「仅 `-host -user` 直连」不同：要有名称、补全、脚本友好。可部分 Free、高级子命令 Pro。

### 7.5 竞品参照：FinalShell 官方「高级版 / 专业版 / 云同步」

> 官方说明页（以官网为准，价格可能调整）：  
> [升级高级版/专业版/开通云端同步](https://www.hostbuf.com/t/1081.html)

#### 官方价目（永久功能买断 + 同步按年）

| 商品 | 官网标价 | 形态 |
|------|----------|------|
| 永久**高级版**（无同步） | **¥35** | 功能买断 |
| 永久高级版 + **一年**同步 | **¥79**（推荐档之一） | 买断 + 订阅打包 |
| 永久**专业版**（无同步） | **¥128** | 功能买断（从高级升专业自动减 ¥35） |
| 永久专业版 + **一年**同步 | **¥168** | 买断 + 订阅打包 |
| 仅开通一年云同步 | **¥49**/年 | **须已有高级或专业** |

关键规则原文要点：

- **高级版 / 专业版永久有效**  
- **开通同步前须先升级到高级版或专业版**（同步不能单独给免费用户当「完整云产品」）  
- 同步约 **30 秒**尝试一次；本机用同步密码加密后再上传（宣传 AES256 + HMAC-SHA256）

→ 这正好验证我们的定价直觉：**桌面能力用买断，云用按年**；不是「全家 ¥20/年」。  
你的 Pro **¥88 买断**落在其「高级 ¥35～专业 ¥128」之间，说得通；**不必做 ¥20/年**。

#### 云端同步在同步什么

主机连接、代理、秘钥、命令历史、文件路径历史、快捷命令、命令参数历史。  
→ **卖服务**（有服务器与安全责任）。BoltShell 一人阶段：**暂不做**；若做，单独按年卖，勿和 Pro 买断捆死。

#### 专业版功能（官网列表）

含全部高级版，另加：

1. 终端背景（本机图片）  
2. **查看服务器系统信息**  
3. 快速输入路径（弹窗选目录/文件）  
4. 历史命令左右键选字段  
5. 快速输入命令名称  
6. 「不断增加中」

#### 高级版功能（官网列表）

1. 网络监控（多接口速度）  
2. **打包传输**（自动压缩解压，适合大量文件）  
3. 高级网络监控（进程监听端口、连接状态）  
4. 高级进程管理  
5. **无限制**命令历史 / 路径历史  
6. 「不断增加中」

官网还写：高级/专业功能多靠**调用系统自带命令**实现，不额外改系统——即监控类偏「远端跑命令解析」，和你们侧栏 sysinfo 思路接近。

#### 对 BoltShell：学什么 / 避什么

| FinalShell | 我们 |
|------------|------|
| ¥35 / ¥128 **永久** + 同步 **¥49/年** | Pro **买断**；同步以后另议按年 |
| 系统信息、网络监控进付费 | **不要学**——监控同屏是 Free 灯塔 |
| 历史「无限制」进高级版 | 避免纯解锁卡 Free |
| 输入加速（路径/命令名） | 可借鉴；**Snippets** 更好讲、更好卖 |
| 打包传输、深监控 | 可后期 Pro，优先级低于密钥/导入/隧道 |
| 离线激活 | License 可借鉴 |

| 我们规划的 Pro | FinalShell 官网有没有近似 |
|----------------|---------------------------|
| Snippets | 有「快捷命令」同步 + 快速命令名，非完整片段库 |
| 会话录制 | **未见**（可当差异） |
| 分屏 | **未列在该页** |
| 端口转发 | **未列在该页** |
| CLI | **未见** |

→ FinalShell 付费 = **监控加深 + 输入效率 + 云同步**；我们 = **跨机传（Free）+ Snippets/录制/分屏/隧道（Pro）**。

### 7.6 买断还是包年？（PRD 定价结论）

| 方案 | 结论 |
|------|------|
| **Pro / 支持者一次性买断** | ✅ **采用** |
| 1 年 ¥20 | ❌ 不当主方案 |
| 云 Sync 另售订阅 | 仅当真做了同步（可对标其 ¥49/年量级，另定） |

对标锚点：FinalShell 永久高级 **¥35**、永久专业 **¥128**（[官网](https://www.hostbuf.com/t/1081.html)）。建议 Pro **¥88 买断**，不必卷包年。

### 7.7 企业（偶接，不预建）

开票、定制、审计、国产化适配——有单再做。

---

## 八、与现有文档衔接

| 文档 | 关系 |
|------|------|
| [个人开发者商业化 FAQ](BoltShell_个人开发者商业化FAQ.md) | 卖解锁、埋点、FinalShell 导入操作 |
| [商业化与开源策略](BoltShell_商业化与开源策略.md) | 收入与开源；功能切分以本文 §七 为准 |
| [开源许可证详解](BoltShell_开源许可证详解与选型.md) | 是否开源、Apache/ELv2 |
| [TODO](../TODO.md) | 工程勾选跟 §五 |

---

*文档结束。宣传以已实现为准；P0 完成前主打跨机传+一体化，安全短板在文中诚实标注「规划中」。*
